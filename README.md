# Todo

SPA для заметок и чек-листов на Nuxt, Pinia и TypeScript.

## Запуск

```bash
npm install
npm run dev
```

Тесты: `npm test`. Production-сборка: `npm run build`.

Через Docker: `docker-compose up --build`, затем откройте `http://localhost:3000`.
